import styled from 'styled-components';

export const StyledRandomText = styled.p`
  text-align: justify;
  text-justify: inter-word;
  font-size: ${props => props.theme.fontSize.xl};
` 
export default StyledRandomText